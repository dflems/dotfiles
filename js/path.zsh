# TODO: install nvm with dotfiles setup
export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && source "$NVM_DIR/nvm.sh"

# TODO: install yvm with dotfiles setup
export YVM_DIR="$HOME/.yvm/.yarn"
[ -s "$YVM_DIR/yvm.sh" ] && source "$YVM_DIR/yvm.sh"
